import React from "react";
import ReactDOM from "react-dom/client";
import LegalManagementApp from "./App.tsx";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <LegalManagementApp />
  </React.StrictMode>
);
